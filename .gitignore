# Python
__pycache__/
*.pyc

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Node (only used transiently by markdownlint in CI)
node_modules/

# Per-branch git worktrees (the main checkout stays on main; work happens in .worktrees/<slug>)
.worktrees/
memory_telemetry.jsonl
.agent-state/
.health-backfill/
memory/


# Scorer instrument caches (Homebrew index etc.) — regenerable, TTL-bound
.health-cache/
